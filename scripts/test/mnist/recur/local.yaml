use_gpu: True
device: 0
early_stop:
  patience: 0
seed: 42
federate:
  mode: standalone
  total_round_num: 1000
  client_num: 20
  sample_client_num: 20
  method: local
data:
  root: data/
  type: mnist
  splitter: RESISC45splitter
dataloader:
  batch_size: 64
model:
  type: fedsak_mnist
train:
  local_update_steps: 1
  batch_or_epoch: epoch
  optimizer:
    lr: 0.1
    weight_decay: 0.0
grad:
  grad_clip: 5.0
criterion:
  type: CrossEntropyLoss
eval:
  freq: 1
  metrics: ['acc', 'correct']
  # best_res_update_round_wise_key: test_loss